These all, are my words, and my words alone that have led us to the creation of what we currently have for an archetecture document. many of them are responses to ai and maybe not pertinant so much now, but this is ground truth for any vision.

do not read it 500 blocks at a time. unless instructed by me how to read it: pull the whole thing into context at once.

Mar 1 2026:

Okay. Gonna describe to you what it'll quick. It is that I want Claude Code to do for me And the Alejandro project. First of all, there is a couple of different git repos skin. My GitHub. The same GitHub as my Alejandro project. Which it should be aware of. Under Inventor 2525. And may come up mistranscribed. But One of them is assistant interaction. And one of them is assistant merger The assistant merger facilitates a Sort of merge request. Type, format, for something like large language models to accept and deny changes And the assistant interaction defines a relatively poorly formatted format with which it can run shell scripts as well as Save and read from files. I want you 2, Learn about those. Try to get information about them, or rather, I want you to write the instructions for Claude Code to do this. I'm I'm using you primarily as a transcription app right now. Your primary goal is to write these instructions to clone codes that I can open up a new instance of you with all of the coding tools available to you. To try to learn about these different frameworks and what I wanna find out particularly is if in the particular web UI version of Claude Code, will you be able to read those files? From those different get repositories without having the ability to pull them directly or will you be restricted by your environment from pulling them. Once we have them, I want to I want you to look at and understanding work to understand required AI as well. It is another project inside my GetRamp inside my GitHub. That one is much more comprehensive, so I've already provided Read Me's in a new branch that I can copy and paste to you. But I will ultimately want to combine those 3 different projects with Alejandro Not merging them into the same project, but just using their components to build a sort of model And this is loosely said because it'll be a model with requirements. That is itself a model So the word model that you're familiar with, you're gonna have to familiarize yourself for what I mean by that concept by better understanding the requirements of the project. But a model with requirements that will break coding into smaller tasks. So as a very kind of meta way of going about programming, essentially I'm going to build out a Much more structured version of things to kind of how you work. Except with Requirements and hard coded processes. Rather than here is bash. Here are artifacts. Here is ability to Do anything you want in this little environment and letting you just go nuts. I want a more restricted path And I'll describe that later. But I wanna get you started on that much.



just make sure you have a very thorough understanding of each for now.... trying to load you up with the research.... look at the smart model server and client and assistant interaction stuff that is inside Alejandro/models. run tree.... know your directories. ultimately.... I want to create a more stuctured code change process...... verify with llm any bash blocks by a model using requirements...... coding requirements not just llm requirements...... any change? each dif is reviewed...... each change that was not important for the request is removed.... the changes are re verified by the drafter model..... coding requirements are checked (eg, did it doc string, did it make a function that is not a good function like only used in 1 place, did it duplicate code, sytle guidelines, did it over use conditions or try catch.... did it do more than meets the task and overcomplicated it, etc)..... things like push only done after such review prpcess or tests.... user controls for taging current state...... run with what I have.... see if you can get my vision and write an artifact for yourself describing all of what youve learned thats pertinent to this, and another that we can use as the 'goal' that you are currently lacking, that we can iterate on. ask me clarifying questions.... I will try to describe this better latter...... but 1 thing
i did think, design wise..... a code requirement is a py file...... it has a doc string at the top describing the user story of that requirement.... it has a validate function that says if it passes given some params like file path or diff..... and a pertinent function saying if that requirement applies (eg, only apply to this module or py file... or if there is a thread or async usage)....... block comment of file is machine parsable, it includes author for tracability.... ai can create proposed requirements but can not mod human ones..... eval used on requirement pys..... project requirements become a dir tracked by git as part of the project, eventually along with all conversation with ai that led to their creation and the dev of the whole project..... everything must be tracability..... my time is valuable because latency.... yours is expanding ever more..... goal will be that all requirements user stories and ' artifacts' that I (or human) makes, are word of god..... to be studied repeatedly and at all cost of ai...... code can be re built from scratch from those artifacts only and selected from, so they must be well tracked and attended to. user home docs also have requirements for global use... like don't apologize or talk up at the llm level.... but.... always use tabs or camel case and big doc strings

You need to focus more on high-level architectural questions. I have no idea what the fuck no unrelated changes is or what do you mean by it getting its task description. Moving on to your second question, sandbox for validate. Validate and pertinent each should be able to make LLM calls. The pertinent imports should be included at the top like a standard Python file. we should be discussing format and what signature should be going into each one of these functions but generally they're going to essentially have to be able to have access to the full required AI framework so that they can perform tasks with specific models and generally how I would see this being defined is you would have a code block up at the top that would define author and not just human or AI but what AI or what human. It doesn't matter what the author is. The author in my case is Charlie Mellenbeck whereas you are Claude and maybe there's even a version number appended to the end. And information about the date and time that it was created, etc. you should never use my name, I should never use yours, etc unless I'm explicitly telling you to create one on my behalf in which case maybe you could specify it as me but in that case I would want my words copied to the description of it verbatim, absolutely verbatim and ideally any recordings would be referenced in the requirement as well as to where my transcription had come from. So the way I see this is you have several different... and I'm going to re-architect this entire system at some point to better suit this vision. I didn't start with it initially, but it should be thought of as you have the audio layer, the input-output to the audio layer, which could be speech-to-text or text-to-speech, or even just a voice AI model, but that then communicates with the application which can run commands Those commands are a command stream So each one of these is a stream You have a stream of all of the audio that should generate a stream of any transcriptions or text being spoken that should all be time stamped Everything should be recorded and traceable there should be the commands being run through the application so anytime you know like an event for push button or some such thing happens anytime a tool call from an LLM from an agent happens, anytime there's a pen touch on the screen. One of the things that I'd like to do in the future is the human can touch the screen with with a pen tool and at the same time be able to speak something like this is a straight line and it has a constraint at the beginning or at the end in plain English and have the English in parallel to the actual drawing events so that a large language model can act on them use that to build essentially a voice CAD program. Everything should be treated like the Linux operating system at the core of things, in at least so much as the realization that basically everything is just memory and rather a stream of memory. But you have these different sort of swim lanes of different I.O. paradigms. all interacting and yet they should all be traceably recorded and replayable you know ideally I would be able even to go back, play an application data stream that is the combination of all of those different things back again and say, no, that's not what I said. It was mistranscribed. This is what I said. And continue forward replaying that entire stream of events so that I can correct whatever mishap or going downhill tumbling on garbage that might have occurred after that mistranscription or due to that mistranscription or misunderstanding so that you know an interaction session with us becomes a large-scale artifact in and of itself composed of all of this different recorded data that we can then just replay in a sandbox and you know even your contributions where you know like if I told you to create a new project and add such-and-such files to it and describe what those files should do and what it is that I want the application to do like we would be able to replay that and the sandbox would end up rebuilding the same file tree over again. Or I can kind of as a meta experiment tell you, no this is actually what I meant, and have you go and change the data stream, the recording of that entire event stream from our session, and replay that and end up with the new corrected version. and when I say traceable I mean that when I specify a requirement via a couple of set of controls where I say some new requirement there will be an agent perhaps that might help with certain tasks in the construction of that requirement file actually writing the code for me but that when I say finish requirement or some such other control my verbatim description is referenced at the top of the file along with the time stamps that would be useful in order to retrieve from recording my actual words so that if something were to ever change in the future or some requirement was to ever conflict in the future in some unexpected way and we realized maybe that requirement wasn't what I originally intended or maybe I want to hear the point in time where I said that something should be done. Maybe it jogs my memory a little bit better to hear it in audio. I would be able to browse to that requirement the point in time where I said it and emotionally, like mentally better, like remember what I was thinking when I said that, as well as be able to fix any mistranscriptions or be able to annotate then that recording, transcriptions, the requirement as me talking to you saying, no, this is, this is what I actually intended by that You maybe didn get my vision correct correct so no these validate function the pertinent function they're all very very high level like that's why I'm using Python and not just like trying to define in some set of classes that with settings let me define what it is that I might want to do in my requirement or how it is that it collects information or determines whether or not... Just use Python. Simply use Python. so we may want to have some form of context object that has things like an already initialized required AI client or some such similar thing being passed in I'm not sure what the signature should be to these specifically but that's the overall vision right looking at your next question AI cannot modify human requirements mechanism I I feel like that could be implemented any number of different ways honestly it doesn't really matter how it's implemented the requirement is that it cannot modify human files we're getting down in the weeds again, this is too low level, think about high level architectural questions that you can clarify with so this would be a new screen though within the you like UI location and it can just be like I don't know. Manual coder. That doesn't matter too much right now either.

I have another summary made by you of the Required AI framework in a different branch in Required AI. You may want to reference that. just make a note of that in your codebase context I don't know how important things like pyenv are to all of this but what you've got looks like a pretty good introduction I'm going to look at your review or your structured review goal. One thing I am I'm going to say right up front is you have kind of these phases. Let's not focus too much on phases yet. let me look through this and I'll see if anything that pops up
so again human versus ai it's just author um the idea is that like you couldn't just go and modify a requirement for me so the way i see that being implemented i guess to be more clear because you did sort of ask. Like, I want to be able to tell you this is what I want for a requirement, but I don't think that I can just trust you to do that out of the gate, because, like, this first paragraph, as a developer working with LLM Assistant, I want every code change, blah, blah, blah. just doesn't sound like me. It doesn't sound composable. I don't even know that that is really part of the vision as much as that I can define new requirements or I can define new user stories or I can define new tests and that those are my primary contributions along with my spoken word being sort of the traceability backbone to those different artifacts. You know, I want to be able to create different screens and different controls or, you know, and when I say screens and controls that's a paradigm that I kind of want to change at some point. like I want to be able to just have a website or an HTML page that we understand that when I speak the word that is the button it is clicked unless there is a property attached to it that says that it is speakable. Or if there isn't a property that is attached to it that says speakable, just like you would attach border style, like you have an HTML tag that is button, color, width, whatever, one of those things that you would pass in, it could be speakable or keyphrases instead of the concept of control that I had, which is basically a verbal button. And now you're more extensible to things like sliders and other things where you can build both a rule-based way of interacting with the system and an LLM-based method of interacting with the system. But the whole concept with the controls or speakable buttons initially is just that it's a simple way of, you know, without having to have like a whole verbal parser that might be able to handle things like, you know, like a sort of pseudo state machine for things like sliders where you say, you know, volume and you say up and up is now understood to be attached to the volume control and having to have like a full verbal syntax tree that you're parsing live from the transcription, you know, at least with just a verbal button, you can do something like, say, new requirement, and that might be a control, right? And that new requirement control, just to talk you through, like, maybe that particular control might be a model, which again a model and this should be something that is in your sort of structured review goal or your I don't rename structured review goal this is like vision or design architecture document something that really speaks to this is how we're going to build this entire thing right the point of such a button or the how such a button would work is you would have a model and like i said model should be in this document or what i was trying to say when i got sidetracked model should be in this document and a description of it that says A model is, and you don't have to copy my words verbatim, but it is to be taken, when I say the word model, not just to mean LLM. I am meaning requirements and the model built together. And when you're describing the architecture of required AI and the entire system, one thing to really note is that these things are nestable. so like a required AI model which is maybe an anthropic model or a grok or a local LLM like an olama kind of thing a required AI model is one of those with some requirements and some ability to only take certain portions of the context like tags in order to filter the context with in a basic sense and an input config that lets you only grab so many messages or do slices of messages, basically doing slices on the entire conversation history like Python slices. But it as a required AI model is itself just an OpenAI endpoint compatible model that can also be passed additional requirements in the call that's kind of beyond the scope of the OpenAI standard. I've basically added the requirements field and some other things to the standard, at least within this framework, obviously. I'm not adding it to their standard, but... A model within required AI can be built on a model within required AI. so you can have something like a talk model that is do not apologize to the user and be serious do not provide a conclusion for your responses for instance those might be its requirements you know it would produce very direct output that doesn't maybe sound too disliked speech would maybe be its goal or its overarching goal of whatever requirements it's built out of but you might have another model that is built on that model and it would have requirements like only talk about medical information never talk about programming never attempt to write code and if I then ask you or if I were to then ask that model you know fetch for me all of my latest medical history, it wouldn't try to write Python or something to go get that. It would just say, I can't. I don't know. Very abstract example. I'm struggling to come up with good examples, to be honest. But let step back and focus on this this new requirement control right it would have a model that would take what it is that i say and be given a description of required ai of abstract AI and the entire code base where it is that it should put things and it would be told write me a new requirement. And then that conversation history might be passed to a different so-called model that would have the specific job of writing the script for something like the terminal to actually place that code into the current directory structure. maybe for instance because that sounds a little bit like overkill right maybe for instance I wasn't saying add new requirement maybe I was saying modify requirement. And so now, that model that goes and modifies the requirement code has to validate that you aren't trying to say that you're me. Unless I said to write this requirement or to modify this requirement. So rather than just giving you the terminal directly, you're first given the task, like an LLM such as yourself. I say you, but I mean an LLM in general. You, an AI, rather, is given the task, write the code. And I don't want to give it the whole of, like, don't use my name. or this is how too much other stuff works. I don't want to tell it too much all at once. I want it to have what it needs and only what it needs to focus on the task of writing the actual code. I don't want it to have to have system prompts about like how to go about interacting with the terminal. I want it to be able to stay focused on this is the code for the requirement and the next model to do things like validate that you aren't modifying a requirement that I wrote unless I told you to modify it and you aren using my name unless I told you to write it And these sorts of models might be useful in other places as well, like if you, while coding, say I have a code change model where I tell it write this code change, maybe that one doesn't get access to it. It can't write requirements on its own. It's never even informed on how requirements work. it is simply informed that code requirements failed as it writes code and it has to then fix its bugs or things that it's introduced requirements that it's violated but if you have another model that is able to take an entire application specification. The user stories, the requirements, the vision, and whatever terminal it has available to it. whatever environment it has available to it similar to how yourself maybe it's literally just you if it is busy building this project and realizes maybe there's a new requirement that might be useful it can go ahead and draft one but using the requirement drafting models or modification models that I just described and it would therefore go through all of the same sort of procedures. Maybe it just gets a tool that's add requirement and that's all it knows about is it can add a requirement and describe what it is that it wants that requirement to be and it doesn't directly write requirement code but that requirement drafting model now creates code that defines that requirement and ideally then that requirement tool would expose to the large model like yourself nothing about how requirements are specified in Python on, nothing about required AI, nothing about Alejandro as a project or any of its dependencies, you might simply get a tool that is proposed new requirement. so that all of that stuff is kind of abstracted from you. Anyway, do what you need in order to update these files. Like I said, rename this one because it's kind of an odd name. but correct what might need to be corrected in this and then I'm gonna keep reading it right now before I send this but I realize that was kind of a lot hopefully kinda tunes your matrices a little bit in the right direction it's kind of wordy but
A little correction on AI time is cheap, human time is not, latency is the bottleneck on the AI side. No, it's not. See, ICU is infinitely parallelizable. Parallelizable, in case that didn't come out in transcription. I can just add more of you. you have to wait for my words to come out. My words are way slower than your words. My ability to read is way slower than your ability to read. So while you are limited and I have limited finances that I can dump into you, hypothetically as this grows, that becomes Dyson's sphere. size. Like, no, I am the slow thing. You are the fast thing. So you have to wait on me. And you might not be able to run very far without me yet, relatively speaking. But no the point is to not have to re-say things if I have to re-say something if something is mistranscribed, misunderstood misrepresented, then that is time that I have to spend re-clarifying something whereas you could be programmed to utilize more GPUs, more power and just re-review what it is that I said and really drill on it. Ask yourself repeatedly is this what the human meant? If the human meant something else or we don't know what the human meant that's a good question we should be raising to the human. But really almost anxiety level review like thinking not thinking but producing more tokens around is this what the human meant really for sure do we actually know can i ask 10 agents like a court case like a jury and they all come back with the same answer or do they come back with different answers and if they come back with different answers it's probably a good question for the human but if they come back with the same answers then run with it and if everything is generally coming back with the same answers then whatever description it is the human has given that should be utilized to create drafts. Ultimately, these different artifacts that the human creates or that the AI proposes should be viewable as a smorgasbord to the human so that the human can say these different AI requirements these are good. These may be stochastically, turn them on and off. Or these may be get rid of. or these may be hold in review and only apply to this draft but then try drafting again with them or with and without them ideally that would be describable verbally but the point of the controls is to add some determinism in that like I can say I want to add a requirement and I can make sure that my verbatim description is copied or transcribed into a Python file that represents syntactically what I mean by having verbally described what is then directly in the file so that if it was misrepresented in code, at least it is represented correctly in text. and even if it was mistranscribed the recording of me having said it is referenced so that if there's ever even a better transcription model then maybe it can be salvaged or retrained on when the user then annotates it and says no this is actually what I meant but ultimately it would be nice if I could tell you verbally looking at requirements on the screen these are things that are God I have said them, they must be applied these are things that I want you to apply to this revision and call it one thing and this revision and call it a different thing. One gets it and one doesn't get those requirements. And then have you rebuild from my artifacts and only the artifacts the entire code base again. so that after you have finished, I can review each of the two drafts to the project and be able to see, you know, nine times out of ten, do you get what it is that I meant? Do you build something that is what I meant? And maybe you can't build an entire large-scale project yet, but it's basically like I would be able to tell you, build an airplane, and you would give me 10 airplanes and I can just choose which one it is that I like the most or find out what it is that I like the most about them. Maybe in one of them, you know, you really added a really great tail fin and I say there is a new requirement. This tail fin must look, and I describe the tail fin and what it is that I like about it in that draft that you came up with so that I can tell you okay now redraft it and you give me 10 more airplanes but where all of them have that tail fin so that as an iterative process my contribution or as a human contribution now becomes defining exactly what must be or ideas that I have about what could be or the overall vision about what I'd like. And these must be separate. they must be understood to be separate but that ultimately we can come up with processes together where you're not just a large language model you are a set of code that is different models quote-unquote, models having requirements that interact together to produce very deterministic results using very minimalized tasks for large language models where the large language model itself, just the weights is only ever tasked with very small things relatively speaking large language models key realization here maybe should be well described in your document a key realization or the key realization is that large language models are great at Q&A and they are horrible at build me an airplane and they always will be that's not something that's inherently like going to change. Maybe one day a large language model or something like it can as weights only build an airplane. But at the end of the day if it can only build an airplane it's not going to be able to build a city. And so in order to be able to build infinite abstraction it must be able to extend the processes that it is using to perform the development tasks that it is doing on its own but with prioritization and traceability about how it is extending it or how it is being extended where the human has the ability to directly contribute without the opportunity of the large language model to misrepresent or misinterpret but also the ability of it to extend itself but with the understanding that it was itself extending itself so that it understands the priority in terms of like at an application level when i say it i mean in the AI itself as a whole. I'm talking about the program, the software at the high level that is the culmination of any large language models or APIs that they are facilitated by as well as whatever requirements that are built on top of or utilized in connection with those large language models. That is the AI. That, the entire program, is the AI. and it must be able to extend itself in code and build new pathways so that it is more energy efficient these large language models being very good at individual single tasks that is where they take the least energy it is not just about them being more efficient when they are given a smaller task. It is about them generally being energy more efficient. Having you, for instance, look through all of the projects that I did at the beginning and try to understand them and research them should not have been done in language. Maybe the first time it should have been done in language so that it was easy for the human, myself, to be able to describe to you. But after it was done, you should have been writing notes to yourself, not just in the form of markdown files or skills, as they like to be called in the bianthropic, but in terms of code that says this is how you get a new file or a new project and you summarize it so that when I say summarize this project or go and look at this project you then having the list of tools are said okay what list of tools can I use to find this and you extend your own list of tools that tool that you then have is summarize project and it is an agent perhaps in a sense but it follows a very specific process of this is how you call a Git repository within your environment that you have available to you. This is how you pull the files from it. Because, and I don't know if that's clear within this context, because I was running into problems, you know, prior to our conversation here, like maybe in a different conversation, where you weren't able to pull from the Git repo because your environment restricted you from being able to pull any repo that is not the one you're supposed to be working on. And so you had to come up with some sort of backhanded way of getting into it using GitHub user content or some maybe WGit or something like that.

Your format for requirement files looks pretty good. the pertinent and validate function I think might benefit from simply having a context object passed to them because they're going to potentially need more here than what's available and I don't know what all is going to necessarily need to be passed to them but a context object seems the right way to handle that long term because it could grow to all sorts of different things about the application state like for instance if any events might have occurred within the application, like me triggering a control, for instance. And, you know, like I had described, the ultimate vision might be one day, and this is maybe a little bit more out there than this initial version, but, you know, the vision might be to have an event stream of the application, both the tool calls and any I.O. like key presses or screen touches or button presses, navigation events, things like that. and so it may want to search through the navigation events or the control events and say, has the user told us to enable this requirement or disable it? That might be a way to define whether or not it is pertinent and of course you you know, the diff and the file path, that's kind of more like a main loop sort of thing. Or parameters from the main loop. So you might have like the context object that has those sorts of items, like the required AI client or the event stream of the application or the word stream as spoken by the human user so that it can skim them or review them itself. But then it's going to have those parameters that are passed in like this is the changes that the AI has made to review this file. In fact, I don't even know that it's best this is something to maybe this is maybe getting too far down into the weeds to really think about just yet because I don't know if you would want to do the pertinent check on an individual file if that the only time that you would run it or you know like maybe you'd want to run it on a folder as well maybe what's better to pass in is the state of the application file and diff are kind of like the main loop iterating the state so like AI makes a bunch of code changes requirement validator passes each one of the files to each one of the requirements is currently what we're saying would happen here. And that main loop, the iterate through the requirements, the iterate through the files, maybe that's not how we want to do it. Maybe we just iterate through the requirements and we pass at the application state. We say, this is all of what the AI did. This is the current conversation. this is the event stream from the application are you pertinent and do you pass and I almost want to think maybe that the pertinent function then becomes kind of useless because it's just do you pass or not but the nice thing about having the pertinent is like was that requirement even important is then known as a Boolean a simple Boolean so that when the user is looking at the audit trail for this entire interaction this entire development process that occurred after they told the system build it, whatever it is that I've described to you, all of these different artifacts I've given you, build the application. Maybe then the nice thing to be able to display to the user is in this step of the loop, the AI made a bunch of changes. In this commit, the AI made a bunch of changes, and these were the requirements that were considered pertinent so that they can see these are the ones that we're also going to validate and they're all true or false. But that they know whether or not for sure that requirement even mattered to that particular commit. So there's no use in displaying to the user that the AI did not have too many usages of like threading or async without locking if there's no multi-threading whatsoever. Right? I don't know. Moving on, because I'm rambling again. again this is all to get your weights like in the right kind of arrangement for this for you to understand what it is that I'm talking about
I don't think it's important for certain directories to be used. I think more so just different authors for requirements and ultimately maybe some viewer might be implemented one day. but I think requirements, user stories, whatnot can just live in whatever directory structure the user or AI chooses maybe just having a root directory within the project no grouping of proposed versus that or another thing to be hard implemented in any way but you know can be grouped however the user likes and maybe we include the usage of markdown files just to describe the requirements in folders so that you know AI can say these are these requirements for you know the wing of the aircraft and these are the requirements for the body of the of the aircraft and these are requirements for the chairs in the aircraft. But that would mostly be sort of a documentation thing. Just readmes. but the night like read me's in the sense that uh maybe maybe we start including some fields in them at some point just a thought uh so that uh you know if we have if we spawn up an agent whose job is to work on the wing it then can find the application rather can find information pertinent to the wing by structurally reading from the readme files what requirements it is that it should pull or maybe we just use Python for that honestly we could just use Python for that as well kind of like a init file So like you have wing requirements the init file just pulls in all of the code requirements and you create a new instance of coder that is then given all of the requirements from that init file. I don't know, I'm getting down in the weeds of implementation now, specific implementation. It's not part of really the vision as much. But it's an idea.
So I'm having a little bit of a hard time. Just my eyes are glazing over for your new flow goal. I'm going to maybe just sit back and describe it myself for a second. So user comes in, they say, I want to change this code. Or AI comes in and says, I want to change this code. Basically, a goal is given, right? AI is given the AI well the first model is given the sort of smart model the syntax for AI scripts that let it run terminal commands or read files whatever it wants to do it pretty much is kinda you or a very very, very, very maybe syntactically poorly implemented but small version of pretty much you that it just writes a script and runs the script and writes a script and runs a script and writes a script and runs a script. But that's at least that's at least how I implemented it before so I actually did implement this with like Gemini before where I'd be able to verbally go back and forth and I think I had a UI to accept it. I don't know if you see that in the assistant interaction or the assistant merger. I think there's something in assistant interaction in one of the branches, if not the master, that does that. I don't remember how far I got with that, but it was a basic little script that would let me make a few changes on a treadmill. It didn't work altogether too great because the thing is it's looking at how AI scripts are supposed to be formatted and whether or not it has requirements at the end which that one didn't have requirements but whether or not you impose requirements at the end using something like required AI to enforce that it's actually following the syntax and it's parsable such as the smart model does it still has to look at how an AI script works. So this is again coming back to this overarching vision. AIs, or the smaller pieces of AI, the big mathy part, the large language model, should only be tasked with small little things. So we need to break smart model down into a few pieces. User gives or AI gives task. So task is received from some source. First model is kind of like an exploration agent, how you would write an agent. If I were to tell you to go tell me about some code base and integrate it with what I have, that you might write yourself an agent that would go and look at how that code base works and summarize it to you so that you have what you need to then go ahead and make the changes that I told you to. we're going to just integrate that into the code itself. So the first stage is the model that goes through the process of finding what it needs. And so it is given a reduced version of the syntax for integrating or interacting with the application. things like running bash commands or saving files or reading files maybe all it is given is the ability to read files or run very specific bash commands maybe it is given the instructions on how to use bash but there are requirements, hard requirements placed on bash blocks that it cannot save anything it cannot commit it it cannot push it cannot do effectively anything other than collect context and understanding and summarize and the next model is not given anything at all about the assistant interaction or diffs or anything except for the pertinent pieces of the code and the summarization that that agent was given And it told draft a new revision as though I had copied the code base to you myself manually in not a Claude Code instance, but a simple chat window where I just open up an instance of any large language model Q&A style, and I paste my code base to you and I say give me a new version of this file it would be given it like that so that it performs the task of writing a new revision to that file well essentially a rough drafter model or maybe instead it is given the task of this is the goal the user or a large language model originally gave you the original input to this whole process and It says this is what you should do and it comes up with a plan and only a plan and Then the next model is given the portion of the assistant interaction script where you know maybe it receives from that last model these are the paths that you are to modify and so requirements are put in place all it can do is modify those files or add those files so that you know it doesn't have bash scripts or the ability to run bash or anything like that all it can do is modify files and there are requirements in place that it it where there is no change it reads from the file from line to line range using range commands or read from lines whatever it was called in the assistant interaction syntax so that it doesn't trample anything. It reads from the original as a means to produce structured edits. So just like yourself, where you create edits, I don't know how Anthropic went about implementing that, but this was my solution to that kind of, honestly, before they manage to, but I was busy with life shit, so I didn't manage to build all of this in time. It's not really surprising someone beat me to this. But like, you produce an edit by reading from lines where there is no change, and then writing code where there is a change. and then that script by the application is run. And so the next model is maybe given... So those previous models were given a much reduced context, right, to keep them very focused. Maybe they weren't given the entire conversation. Maybe they were only given the summarized goal or the plan. but that next model what it does is then take the diff that is produced and using the much more full context of the entire conversation basically make it appear to that model as though we're back to like what I was doing, like I was saying before, where I'm just a user pacing code into a chat and saying, change this file, make the context for that model look very similar to just a plain conversation. It knows nothing about AI responses or syntax or anything. And it's asked simply and only, does this hunk of the diff that was produced, like this is the file that you changed, and you changed this file. So this, you know, maybe you changed these three files. You know, like maybe the draft model, make it look like the draft model's conversation so that maybe it thinks, you know, maybe you construct its message from scratch, its prior message from scratch in code so that the context looks as though it had instructed you out of whatever loops whatever programming had occurred that made it produce like 10 different file changes it thinks that its previous message to the user was giving it literally the con or giving them literally the content of each one of those files and the user then comes back and says this is the diff to this one file did this particular change that did this one change need to occur or does it need to be changed in some way does it need to be handled more like a merge conflict. And if the model says, no, I'm sorry, that was not something that needed to be done here. Let me provide you the new block. A requirement says no. That model, that final model, has a requirement that says you will only say yes, no, or provide me the content of the block that you would like to replace the current block's content with. And the application is the thing that then writes the apply choices format. So that, you know, you can ask basically that very wise model that feels as though it had looked at the changes that it made as though they were simply a chat and it feels like it is only being asked the simple question did this change need to occur? This particular diff, this particular hunk of the diff or did it need to be replaced with something? and that context is then something that can be cached. That context is something that can be cached and run very cheaply for each hunk, each individual hunk, so that it has a very direct and specific task of just validating that hunk. And then once all of the hunks have been validated or replaced, accepted, denied, or replaced, then and only then all of the changes are then given to that same context again only you know not with any of the history of looking at each one of the hunks so now its context is your entire conversation leading up to the point where the user said or the app or the ai said do this for make this code change and then it's a procedural message is generated that is to take the role of the very wise coding agent that makes it look like it had said here is all of the code changes that need be made you save this file mark down code block you save this file, mark down code block as though it had come from just chatting with the large language model rather than code changing like agent just to make it look like a very Q&A style kind of response that is the portion that would be cached for validating each one of the hunks as well as for using for the final step which is you give it all of the diffs and after having done all of your applied choices and saying yes, no to each individual hunk and whatnot, you ask it, is this good? Is this what you meant? And it's given new requirements, only this time they are to say reason about your response first do not do not say yes or no until you have reasoned about your response so as to not put words in your mouth and then at the end in a very structured and parsable way say yell me yes or no so that it can be verified that the large model believes that the application going through this process has reached a consensus that these changes were in fact the changes that were intended to have been made and that no additional changes have been made or no erroneous changes have been made or no changes have been made incorrectly. And then, and only then, do you then go through and validate. So that whole thing that was like a nested set of models right but that nested set of models is your like apply change whereas you now have the coding requirements that can be run after that and running the coding requirements after that going through each one of the pertinent and validate checks for each one of the Python files that we're talking about, that whole process on top of what it is that I just described is now like make validated change model or validated code change model where now it will produce a code change that or a set of code changes or bash commands that will only meet the coding requirements and never, ideally, never add anything that was unintended. Because that applied choices model, the one that's validating each one of the hunks, that model would be given, or that small sub-model, would be given the very clear written requirements that a code change should not occur unless it was in the request. For instance, no spaces should be added. No spelling errors should be fixed. No anything. Because that would create more burden on the human for handling the merge request and validating that that is what they intended to have happen. And it would create more burden for any AI as well, any form of intelligence for doing the same. and so maybe for instance at the beginning of one of those models you have some form of reset function like the the very high level uh validated you know it had run i forgot what i called it i don't know what to call it we need to come up with names for these things but the higher level model that validates all of the coding requirements and uses under the hood the model that just makes sure that changes are applied that only apply to the goal of the edit maybe at the beginning of that large model the largest one there is a reset function that if none of the coding requirements or one of the coding requirements is not met then the entire system is reset to the beginning of that commit maybe it's given context similar to how a perspective response in required AI is redrafted it's given the original or the current prospect and told to redraft it maybe it's something like that where it's given this is the changes that you made last time however they were violating this requirement redraft them or retry but the environment the coding requirement or the the coding environment would be reset to like whatever the process is for that reset you know like if there were environment variables that were set or if there were um you know packages that were installed even or if there were obviously if there were code changes like git would be reset to the previous commit but maybe even the best way to reset the environment would be for it to be for instance a virtual machine that has snapshots enabled and it literally goes back in memory to the point in time when it last restarted that revision.
but you see like the point is the ai is not then the thing that is saying push at the end of that model once all of the requirements are written only then is a commit made so that you know each individual commit was actually following the requirements and was the intended goal of the user so that the commit message itself is actually the message from the user telling the large language model or maybe the AI telling this whole system if it's directing multiple of these itself this is what you were told to do and then these were the changes that resulted from that. And the commit message itself could perhaps even contain the audit trail of this entire thing. I don't know that that's the proper place to put it. But for traceability reasons you know whatever audit trail could just be in a file directory but within the project so that every time that it went through and said this code hunk is not good, remove it or accept it or alter it or this coding requirement was not met retry entirely every single thing that went wrong or that happened is then part of that commit and you know like get large file storage or large whatever the hell it's called um like the audio of the user saying what it is that they wanted to have happen is even added to the project in a particular folder that very clearly you can see right in the log of git this is what was told to have this is what you told it to do and this is what you got.
I don't know that we need to specify specifically requirement categories, although these are a good, I'm not reading all of them, but they look like a good set of examples of what requirements could be. I would think of these as more things that we should define in a sort of examples directory of how requirements are specified. What might be more important to say in the kind of whatever we call this file what might be more important to say is that we need clear descriptions of examples of requirements that can be given to a new requirement model So this new requirement model might contain things or might contain or might utilize that coding model that I just described to you in detail as its underpinning. But it might be given as part of its initial context. So this is what that input config is for because that input config isn't just for taking slices. The input config can pass in hard-coded context from files or from string, rather. I mean, we can create string from files using code, so it just takes in strings. But, like, this would be... These would be perhaps good example requirements that can be used to instruct it or to create its context to say when the user tells you they want a new requirement this is how you construct a requirement and from its perspective again it's looking at things high level it has no idea of a sense in assistant interaction syntaxes its context would look like a conversation where the user says i want this requirement its system message would be the content of a bunch of different requirement files along with instructions on how to construct a requirement and where maybe different projects are that it might want to add the requirement to or what current project is being worked on so it knows what path to give the coding agent or coding model so that it can say the user wants a new requirement in the assistant interaction project and this is what they said this is how requirements are built build a new requirement and it then application parsed or created response that runs through the entire process of the coding model would be literally the contents of the pyfile that is the new requirement topped at the beginning with the path that it should save it to so that it's very readable. It's very human-readable. It's very understandable to AI and to anyone or anything. But all along the way, while it was writing all of that code and modifying those files or changing whatever requirement the user told it to change instead of maybe just adding a new one, you know it was going through that coding process
I don't believe, however, this whole block that you have about how a code requirement should inherit from an AI requirement is reasonable. That should not be. A code requirement is different than a required AI requirement. A code requirement is not a class. it is the Python file that is the docstring along with those different functions maybe it would be useful in the implementation of this entire system to create a class like you have here but I wouldn't base it off of the subclass requirement subclass from required AI but maybe it would be beneficial to have a class that would parse out the author and the description and the date and time that the user spoke the text that is the description, whatever. Maybe that would be a useful model class to contain the parsed object that is that pyfile but it should never be something that really is the key way of defining what a requirement is maybe it's just the output of the parser that looks through the different Python files I wouldn't consider it a serializable object in that it shouldn't have its ability to spit out JSON or some damn thing. The origin story, the key thing, is the Pyfile that it's parsed from. That's the object. And so when I say that it should be called by evaluate, I don't know exactly how evaluate works. I've never used it too much in Python. I've used reflection and a bunch of other stuff in other languages mainly C sharp but my goal, my hope is that you would pull out the validate or the pertinent the pertinent function from that file and you would pull out the doc string from that file and you would call them somewhat directly and maybe it would be nice like i said to wrap like using the evaluate function passing the context to the evaluate function or passing it to the the validate function inside the pi file that's being evaluated maybe that's nice to wrap it that way. But, like, you're not going to have a subclass for each requirement type. You could, but I don't think it's a good way ultimately to define things because it's not as easily editable. You know, like, if you have a bunch of classes listed in a Python file at least to a human it becomes very unreadable because things like the description are going to be jumbly on multiple lines kind of tabbed over where they don't need to be tabbed over the formatting just doesn't look as nice you wouldn't have multiple requirements specified as class instances or as subclasses within a file. You would just have a requirement being a singular file. I feel like that is just going to be easier to read than anything. Because then too, you know, they can potentially include things like full-on HTML inside their description and have it formatted nicely without it being extra indented over in some places like on the first line but not indented over and kind of muddying the class indentation by being at the beginning of the line. Like HTML, when you put it inside of a triple quote, just looks like hell in Python. So having a separate file without a class wrapping it just makes it more extensible format-wise for human clarity. and potentially allows you to do things like at the bottom perhaps even include like a separator maybe at the bottom that lets you say okay from here on out we're in markdown and the parser just removes everything after that before running it into Evaluate. So it lets you have one file that can be very rich in its documentation. It could potentially have an entire code base in it that is describing what that requirement is about. For instance, and this sounds stupid maybe and abstract, but let's go back to the plane example where I tell you, create me a draft of my plane with these three different requirements that I come up with and you come up with ten different drafts of a plane and I decide I really like the tail fin of that one plane if I come up with a requirement I might want to be able to say as an example to what I mean in this requirement here is the entire draft that you came up with and so I might include in the bottom of that file something that you would never see in most pie files that is literally the entire draft in maybe Markdown separating out each individual file that you wrote in order to define that version of the airplane and all of our conversation history and all of our back and forth that went into revising that draft as we iterated on it to convergence of there is airplane for context, all of that would be included, perhaps, just as an example. And I might say in the top of the requirement, there might be a parsable thing within the doc string that isn't just the description, but I might say strategy to the evaluation of the requirement. I might call it strategy, I don't know. but that strategy might be no example and then use example after three tries so that say you're making a code change now in a new revision of this airplane like this requirement is being used now in the new revision of the airplane you have no knowledge of the version where you created the tail fin initially but after three times I'm going to give you as an example hey by the way code changing agent I see that you have failed to meet this tail fin design requirement several different times. Maybe you're having a hard time understanding what it is that I meant by designing the tail fin in this manner that I have described to you as I have described it to you. Here is an example of you having defined it before successfully. This is when we actually defined this requirement. this is what you did that I said I wanted you to do again so that now after maybe three tries you might have a better chance now that you've been given that information and so see when we're talking about file format including something like that example might be very very large or very rich in its content it might have HTML in it it might have CSS in it it might have, I don't know, any number of different things. I want that to all look nice. So I don't want that kind of content to have to be defined in a class. I want it to be definable in its own single file or set of files, maybe. Maybe a requirement is a folder in some instances. and so the parser can either handle single file requirements or it can handle folder requirements where you're given multiple examples perhaps but that's why I don't want to have I don't want to rely too heavily at least, maybe the parser creates a requirement object, but I don't want that requirement object to mirror what's in required AI, and I don't want it to be the primary mechanism by which requirements are defined usually because it's just not as rich as having files.
So, that was a lot. I'm actually going to stop here. Let's maybe... You redraft this file with the better understanding. And then... let's go through new questions because I feel like I might have answered some of them I don't know if I did or not but I feel like I've given you so much context now that again some of these questions just kind of glazing through them look very low level. You're talking about naming convention require.py or something else. That doesn't matter. I've already answered some of that in describing all of this. How are code requirements loaded, eval, and file string? Yes. I don't know how that's done though so much so that's more of a question for you than it is for me yeah don replace the assistant interaction scripting format i want to change it i want to make it more friendly but i feel like whatever the format is is relatively unimportant to the architecture of minimal exposure of that format to task specific models that are not muddying the drafting process of a model that is ultimately completely unaware of the format of the the interaction script that it's using. Automated test running, I see those as a requirement. So, the test pass, the validation or the validate function would then be simply running the tests. That the test pass is a requirement. sounds a little bit kind of relatively stupid but maybe i'd i don't want to have that requirement at the moment maybe that's uh... maybe that's uh... project level requirement because the testing methodology is different for each project how it is that you would run the test you know you unit test would not necessarily be something that i would want to have built into the main loop of the application I might want to have within Unity loading a specific scene and running a specific set of actions within that scene and having results from those actions be behavior trees that are my tests. that would be the requirement that those scenes tests pass whereas the C sharp equivalent to unit test the actual code tests might be a different requirement You see, so each one has its own validate function. That way you can implement tests much more colorfully in how it is that you would build them than simply using a fixed framework that the application is always using. um moving on again your phases just feel like you jumping ahead like what should go in each phase that might be a point for discussion between us afterwards after we have better worked out this document and what it is that will ultimately be done one day your questions rather than technical specificity like whether a file should be ending in dot requirement dot pi rather than just dot pi things like that should not be your questions. Your questions should be what is it that you want to do first and is this what you meant or not meant by your vision. And then we'll dig down into the technical design in more so the forms of drafts because I feel like you can probably build out a file that implements those different models that I was talking about, which those different models that perform coding tasks are the main requirements or is the main task that I ultimately want to accomplish within this conversation.

Mar 2 2026:
 Reviewing the doc during your downtime, one of the things I notice is a little bit of misunderstanding of the process. I want you to specifically look at the coding process, spend maybe an extra agent's time really looking at what it is I said verbatim yesterday. but I made a few edits to the architecture document myself however one thing I am noticing is that it might be worth emphasizing my opinion of tool calls in some instances is the way tools work under the hood, right, is you're aware of this, but you're given a list of different tools, what their purpose is, and what sort of inputs they need. And your job is to then select them and fill in the parameters and get a result back and use that to further continue. However,
 While this is not seen currently in the version of the assistant interaction script or syntax that I am currently using, one of the problems I have with this paradigm is it leaves you too much for decision making and deciding what sort of tools would be best to call, which leaves you so much with a smorgasbord that you tend, and large language models rather, tend to want to use everything or use things a bit excessively. for instance you'll spawn up an agent to go and research something that you're perfectly capable of researching yourself with absolutely no benefit of having you have that run in a separate thread that you simply had to spend more tokens in order to describe to an agent with lesser context what it is that it should be doing and then using its response to then validate it again you're not good you're good at planning you're good at next token generation and the task of next token generation can be reinforcement learned to include the task of planning I believe that that is the case however as a philosophy I believe that that long term a syntax closer to plain English is better processed by an application While its code may be more complex it is harder to ship something that simply works or is useful than something like tools it is maybe better to parse something closer to the plain English that it was seen in something like a stack overflow post. So the assistant interaction scripting language was designed to handle some of the difficulties in and parsing plain markdown. But the ultimate would be to simply be able to handle the kind of markdown-style response that a Q&A agent or Q&A model, large language model, without any requirements, will typically generate. Being able to parse that and say, ah yes, you're giving me a code block for Python where do I need to stick this? where do I need to save it? how do I remove from it the original lines of code here and get it into the intended save file or structured edits that would be nicer to have for the application Bringing the model down closer to its original training data rather than trying to reinforce onto it how to plan around using an unknown set of ever and ever list of tools using instead something much more close to how it would instruct a human to actually perform the task so the assistant interaction script syntax is not, and I want to emphasize this is not a ground truth, this is not something that needs to be utilized like long term. I built a parser around that specialized syntax that has almost nothing to do with OG Markdown, simply to accelerate being able to paste back and forth to a large language model via chat, a free one, while I was on the go, while I was on my phone. it helped me allow it to do things without me having to use a phone screen to type things it has a lot of therefore extra requirements around it that are not things that need to live long term but ultimately the goal will be to build a better parser that can handle something closer to the actual like just getting the stuff out of markdown the problem with that of course is that plain markdown is ambiguous. Closing code blocks for instance is ambiguous. If the code itself has markdown-like syntax inside of it, you don't know where the code block for the actual Python actually ends. And so slight augmentation to it or the usage of a large language model to interpret where the actual end of the code block is would be necessary in order to do something like that But things like reading or using bash, I think it's better to have a limited syntax for it to do things like that. Eventually I'd like that syntax to be closer to just plain Markdown, but I think it's better long-term to utilize a syntax because if the syntax is close enough to the original data, you're not having to reinforce onto it an entire tool calling system. You can simply fine-tune on the syntax and perhaps even much more advanced, not going to be done anytime soon, but you're left with opportunities like calling the logits, and only pulling from the logits those that are parsably valid next token for the syntax that it's trying to write. So as to guide the model and potentially be able to use a smaller model to produce such scripting syntax.

 This is all to say, of course, that in your different stages that you'd use to loosely describe the plan that I have for creating structured code changes, a lot of them you're saying should use tools or not use the assistant interaction syntax. the way I would look at it is usually reduce the syntax and eventually once I have a more complete parser that can handle will change the syntax but I think since the syntax already exists even though it's crappy and might have to spend some extra energy guiding the model through requirements that it's actually using it properly it's better to just give a reduced subset of the syntax to, or a different subset of the syntax to each one of the models used to complete each stage. This also means that non-tool friendly models would be able to be used.
 
  I think your stages may be a bit messed up though because exploration after my edits to it look sort of okay. Planning looks sort of okay again after my edits. Drafting is starting to feel like you might have skipped a step or or maybe just... Or rather, stage four starts to look like you're skipping something because between here, an AI script has to be generated in order to actually apply the edits, or some mechanism has to be able to take the drafting model's output and provide edits, which currently is the assistant interaction syntax, bad though it may be. The hunk validator would then work off of that, and the final confirmation works off of that. requirements validation being a higher level model built off of that whole system then works off of that I haven't read those yet it should be clearer that you're probably using the same context for the exploration as you are for the prefix of the planning context, its input would then be the output of the exploration without any knowledge of the assistant interaction or changes in the system prompt. So the exploration is a system prompt that explains assistant interaction and how to use a limited version of it in order to read files. planning is then well sorry exploration is that system prompt plus all of the context that is relevant like the user going back and forth with the AI high level AI or a prior AI conversation inner monologue if this becomes kind of meta then the planning stage is all of that conversation minus the original system prompt but now with the question added on at the end along with the output of the exploration what do we need to do in order to make this user request or AI request happen. Drafting is then taking that plan and given the context from the exploration model, taking out each individual step in the plan. The plan needs to be a well-formatted machine-readable output that can be broken down into pieces because is now the drafting stage is looking at each individual one of those pieces and attempting to create useful drafts in Q&A style. So exploration is reduced AI syntax, planning is no AI syntax, drafting is no AI syntax, applied choices is missing a stage before it where you have to take the draft and turn it into an AI script that can be parsed by the machine. That's basically where we're taking its markdown output Q&A style and converting it into, okay, this is read lines from file, save this chunk, read lines from file, save this chunk. It's the one that knows intimately the saving and the bash commands of the assistant interaction scripting language, and it takes the plan, the draft, and converts it into machine-readable, non-ambiguous, because like I said, markdown is ambiguous so this disambiguates the markdown that the drafting model produces and produces a machine-readable script that then is applied and then what you're calling stage 4 is the next stage so you missed one, right? That's what I just described. The new, well, what you're calling stage 4, applied choices is then it is the same context as what went into the planning model along with the draft for the file that is being worked on along with the diff for that file and then it is looking at as its final message in the conversation a question does this hunk need to occur And so a key realization here too is that a lot of the cases a system prompt may not be the proper way of doing things. Not all models pay attention very well to a system prompt, and honestly I don't know how that ever really very very well could be fixed. Some of them have seemingly done very much better at obeying that prompt, but it's just easier, in my opinion, to have a stream and the most recent thing is this is the current task at hand, do it. And that is your sort of system prompt, is the last message in the conversation, and that last message in the conversation is only seen by this model. That's part of the reason why I implemented a tagging system, because I think that it's more valuable long term that a conversation stream like this, like the exploration phase, is given a message. Maybe it's not the system prompt, maybe it's the last message in the conversation. It said here's a reduced version of the assistant interactions scripting language. Here is how you read files. Now find out what it is that would be needed in order to complete this goal for the user within the project. Plan your steps carefully, whatever. And that system message to it, which is at the end of the conversation, would be tagged that this is a prompt to the exploration model from the application. and it is not to show up in anything other or anything else's conversation history. The planning model then, as part of its input config, can filter based on tags and it can get rid of or ignore that tag or rather only pay attention to the tags that it's interested in. Same with the drafting model, same with the applied choices. Each one of them creates or has input from the application, which might be like a tagged application message, and it filters out application messages that aren't the most recent one, for instance, and then applies its own tags exploration, planning, drafting apply choices disambiguation or conversion to AI script language each of those stages generates a message that is tagged with its own output so that the next model can just filter on it and you have one conversation stream that then is very easily cached by any API provider Because very large portions of it especially if it a stream like that where the beginning of the prompt is not changing, is now cacheable the entire way through. so there's hardware math optimization reasons for not changing the system message for each one of these models and just continuing with the same context through the entire path but moving on to choices yes, then it looks like a conversation history where it's asked, does this chunk matter? Continues on with that chunk. Good. I'm not going to read through all of what you have here because you already kind of missed a step there. Final confirmation. It is given all of the diffs. and again it looks like the original context because you're filtering out the application messages except for the most recent one you're filtering out the exploration the planning the drafting or maybe you include the exploration but you filter out the planning the drafting or maybe you include the exploration and the planning, but you filter out the drafting, the applied choices, and you give it the question, is this what you intended to do? I'm not sure what the best context for it, that's not pertinent at this moment, but, you know, then it's, and you know to each one of these outputs say the apply choices its output is to apply this choice or not after all of the different instances because that happens in parallel multiple different agents get to ask the same question for different hunks once all of them have been applied or changed or whatever then it very much could take all of that output and the application could provide an edited message that is the current status of the files now that everything has been changed. So it would automatically construct a new message that being tagged from the application but for information purpose would then be useful for the final confirmation model.
  
   Looking at your understanding of controls, that's a bit misunderstood as well. I'm not going to bother correcting that one in the file just yet. You can go ahead and just restate this. But you have the current concept of controls generalizes to a speakable attribute or keyphrase HTML element. yeah so we're kind of looking at a couple of different things here so you're saying this gives determinism to verbal input without requiring a full verbal purser you see the thing is is ultimately I'm thinking a full verbal parser is required. So like maybe maybe you could just implement buttons as HTML with something like you know a speakable is true or false or key phrases for what different phrases could be spoken that would trigger the button that's pretty similar to what I have right now with controls in Alejandro. The control class, specifically, that is in Alejandro. But that doesn't let you handle things like range. Because, you know, when you speak the name of the slider, that puts you in a sort of state where now you have selected the slider and now you have different commands available to you like up, down, max, min, certain percentage. You could speak a number and then say the word percentage. That would have to be parsed properly. That's not like a key phrase that could be spoken or put into an attribute easily. it not a fixed phrase it a verbal syntax so at the end of the day you probably going to have a verbal parser that long vision is the combination of you have elements added to HTML tags that allow you to configure the verbal parser and you have the verbal parser. My current architecture doesn't utilize the HTML tags at all and everything is handled in JavaScript and Python which is kind of just an interim system. I want to emphasize that it is an interim system that I don't intend to rebuild just yet, but I think the long term is the HTML tags with new attributes specifically to configure a verbal parser. I don think that there a stage that we build like controls out of buttons using HTML to configure control objects or anything like that I think we just go straight to having a verbal parser but that not part of what I want to work on now I want to have that in the architecture document but I want to be able to tell you once we have a good architecture document that what I want to work on is the code change models or the models specific to code changes and the code requirement checking with the ability to add verbally new requirements. That's ultimately, that will ultimately be the task, the first task really to do. But I want to have this architecture, because I've already spent a lot of time describing the overall vision. So that's what we'll add later in the future once that piece works.
 So to clarify with your new requirement control There's a little bit of a mix up at the end of 3, beginning of 4 Current looks like, or context looks like, here is how requirements are structured Here's what user wants, write the Python file not given full pipeline mechanics assistant interaction details. That doesn't go into the context for the new requirement model. The new requirement model would be, or the new requirement coding model would be part of the new requirement control, and it wouldn't necessarily be using the assistant interaction syntax but I want to be clear when you say its output goes through the coding pipeline that it's not like you know it goes through this process and then it goes through the coding pipeline I want you to understand that the new requirement coding model is based on or its base model is the essentially the coding model. And the coding model is based on the code change model. Code change model knows about assistant interaction syntax. It uses it intermittently in pieces, going through that whole long process that I described earlier to make a code change that doesn't add additional unwanted changes. changes, the model above that, the coding model, uses the code change model to impose requirements that are only validated against once they determined whether or not they pertinent In this case some of those requirements will be how to define a requirement Some of the requirements for defining a requirement might be that you have a YAML parsable docstring at the top with certain keys that describe the requirement. And below that, you have two methods that each take a context of a certain type that will have to be imported that facilitate you doing things like talking to an LLM client. and then that requirement that new requirement coding model is based on the coding model so that you know it is given examples of how requirements work initially but because it is reusing the coding model its context is automatically under the hood being appended to and filtered for what is necessary in order to produce a code change that meets the requirements of how to draft a requirement. so it's not so much like going through a second process afterwards as it is because it is using the coding model it's guaranteed to get good code relatively speaking I mean the requirement verification large language models could potentially be systematically bad in some way, but generally speaking if they're given small good questions that validate whether or not the requirement is met, then it should be pretty robust. Much more so than just asking a large language model give me code for this project that does blank change.
 the rest of what you had for new requirement control is good moving on
 I think some of your early points to under what this means architecturally in the airplane analogy, why it matters, are good. Maybe the first and second one. And the third one. The fourth one, AI-initiated extensions to the pipeline are possible but carry lower priority. that's kind of you adding that. I don't think it's lower priority for the AI to be able to add more circuits. The way I think of this is coding, the act of coding, is an efficient way of utilizing Turing machines, computers, for what they are good at. Producing deterministic fast computation for specific tasks Real human neural networks brains of all species grow new connections They don just adjust the existing synapse strengths so we need AI to be able to do the same thing and the way I think that that should be done could be done is that it grows not just in weights or in architecture of the weights and layers and arrangements of them and the sizes of matrices but in terms of the code that is utilizing them and orchestrating them. If there is an efficient path for instance to pulling a Git repository within a particular environment then it should not be something that has to be figured out or discovered how to do again. Now, that's a pretty dreamy statement to just say that that would be what would happen. obviously we would all like that but doing that or achieving that in the form of tool calls where there is an ever-growing list of new tools seems insufficient better to think of it more like a growing language where the AI has different tools available to it within different states that are like scopes within a language that it can self or that can be extended by the human through the creation of new artifacts requirements being one of the primary ones but to or also user stories and tests and logical statements that should be reasoned about or verified mathematically or empirically I don't know the correct terminology to describe all of what it is that I'm thinking but all of the different ways to capture intent or wish or wish
 but it's not a lower priority that AI be able to self-extend as it is so much that human provide good grounding in way that is not editable or muddiable.
 One such artifact that I am thinking as I'm reading the smorgasbord drafts and iterations or iteration is your line about not implementation details. The process converges iteratively with human contributing only their preferences, not implementation details. See, I don't like that too much. Now granted, AI will eventually very likely be able to far outpace in terms of deciding implementation details. But, one thing that should be considered is, as an artifact, the concept of ideas. If the human has an idea about implementation, maybe a set of revisions is set to run with the idea as an input to their development process and a set of revisions is run without the idea as input in order to try to add some greater breadth to the exploration and some greater stochasticity Even if the idea is bad it may lead to other paths of exploration that would not have been as likely A large language model, for instance, will typically be very bad, for instance, at random password generation we're seeing, because it's always picking the next likely token, it really only tends to favor maybe a small bucket of some 20 so-called random-looking passwords that look to a human to be random but are in fact not. Such things also happen with ideation, where I don't know how well it's been tested yet, but I'm sure it happens by that example alone that having other input for the ideation process about how to solve a given set of requirements and condense them into a work and solution, having the ideas or not having the ideas could generate a much larger set or a much more differing set of revisions. So
 You added a what should be in the examples directory. I don't know that there should be such a directory. some mechanism might be needed to pass examples into a new attempted revision So, you know, say we go back to the airplane example. I ask you, given these set of requirements, build me an airplane. You give me 10, and I pick the one that I like with the tail fin, and I say, you know, this is good tail fin requirement. It must be like this tail fin. that might generate in some revisions an example directory that has the audit trail for that revision so that you can or can't in other revisions be able to see it to aid in meeting that requirement if ever you have a hard time meeting that requirement if the requirement itself wasn specified well enough what kind of or how that tail fin looked and it getting stuck then it might be able to utilize that example after some time to meet the requirement but the implementation detail about how you pass in the example um i don't want to decide too much what format of that looks like yet. I am thinking though that it makes much more sense that the requirement itself could either be specified as a plain Python file where the top of the document is YAML, as I had described in a content block, very similar, like literally identical to what that you have currently, but I'm thinking that that looks very YAML-like to me. I don't know that it officially is or meets it perfectly, but it looks good YAML to me by eye, by memory. But moving on, then you have those two methods, very similar to what you have now, except I would import the context type and use proper type hinting everywhere. in fact my preference for that should be in the architectural document but then perhaps instead of having you know a dashed line comment at the bottom of the python file that allows you to separate out markdown that contains your example perhaps instead of doing that it would be better to not have anything past those two methods and if there is such an example then perhaps that is an optional argument instead within the YAML comment doc string at the top that allows you to specify where said example lives or or how to load it. For instance, pull this branch of the project that is at this location and load from there. Maybe it's a loader function that loads the example. In any case, I think having something more generalized would be better. In fact, thinking about how this works, it may be better that instead of comment with YAML-like, it is a proper docstring, the three quotes for the human written or original AI statement about the requirement, the intended verbatim transcription. and for the other properties to instead have a variable that is either a class instance that's constructed like a data Python data class, decorated class instance that you set the variables of or like a Pydantic instance that you set the variables of or you know I fine with dictionary I just don like it because type hinting and whatnot doesn work in an IDE and syntax highlighting doesn't work as nicely, it's just less safe, but one of those two, ideally for me, I prefer Python data class, but just because it's built in and it's simple, but then rather than having to parse it with a YAML parser it's already right there and you can include a loader for said example and perhaps then not necessary to its function as a requirement but available as kind of a convention, it might be better to then specify or to create a folder that is the name of the requirement and then inside that have the requirement Python file along with anything needed for the example or any other readmes and whatnot that might be helpful in guiding towards meeting that requirement optionally. And that might even contain an entire git repo, for instance, or a loader bash script to it.
 Moving on to your questions, what does the context object look like? Well, I described my preferences of a usage of data class to define it. I don't have too much specific to tell you what to put in it. Just put in what it is that's needed and it will grow with time. That is to be discovered through later development, not defined upfront. how does the coding pipeline compose with the existing assistant interaction intent is eventually minimize how much the individual knows about the scripting format but what is the migration path the migration path is that eventually as i was saying we will move to markdown and it will be disambiguated with AI, large language model specifically, into parsable format. So draft model comes up with markdown as would be a Q&A model and LLM is used to disambiguate markdown non for such tasks what triggers the pipeline Is it a voice control A UI action I would call it a control. It's a voice control. Make this change. the way I see it is again there is a singular conversation and you can spawn new conversations at will but there is a conversation that contains all of this system messages and application messages and human messages and AI messages that are tagged and filtered on and you know I might go back and forth with you with some code in your context and talk about different changes I want to make, but then a voice control, I say, make this change, and it now goes through this entire autonomous coding strategy to build out the actual change and commit it. Does the replay mechanism work at user-facing level? I don want to dig into that that a future vision what does the smorgasbord UI look like That a future vision Where does AI propose requirements live relative to human authored ones? Same directory, same folders, they're just annotated differently. UI this is simply a text editor. How does a model know it's building on top of another required AI model versus a raw LLM? Is this transparent in the API? Absolutely not. A model built on top of a model is completely unaware that it is built on top of a model. As far as it's concerned, it is the large language model. to I mean look at look at how the required AI response drafting process works The end result is a prospective response that meets all requirements and that is what is appended to the conversation. And the extra fields that are attached to that message, like the prospects field is not directly visible within any like open AI chat style standard conversation format so any large language model that's sitting under the hood is receiving only the final prospects in order, along with the human messages in order, it doesn't receive all prior prospects. It has no idea that it went through a requirement verification process. If I gave a simple LLM such as yourself the requirements do not apologize and never produce a summary of your response at the end, then the conversation would look as though you were simply an asshole and you had never apologized and had never summarized that you were simply direct the entire time. You would never know.